- Chức năng Input để qua Control hết.
- File SaveLoad.h/.cpp có 2 hàm để thực hiện chức năng save and load.
- System('cls') giờ k xài cái này nx đổi qua ClearScreenWithColor (Hàm này xoá màu nền cũ và thay bằng nền màu theo tham số của nó) và ResetColor(Reset thành màu chữ mặc định của console này chính là cọ vẽ vẽ bàn cờ chữ các thứ).
- View chỉ để vẽ k được có xử lí input.
- GameAssets.h/.cpp là file để các ảnh vẽ.
- Vẽ các hình ảnh back ground nhờ file script python.


